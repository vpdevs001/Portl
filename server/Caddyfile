portl.vedpandey.in {
    # UPSTREAM defaults to a host-run server; docker-compose.prod.yml sets it
    # to server:8000 (the API container on the internal network).
    reverse_proxy {$UPSTREAM:localhost:8000}
}
